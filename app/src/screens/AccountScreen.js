import React from "react";
import { View, StyleSheet, Text } from "react-native";

const styles = StyleSheet.create({
  h1: {
    fontSize: 48,
  },
});

const AccountScreen = () => {
  return <Text style={styles.h1}>Account Screen</Text>;
};

export default AccountScreen;
