import React from "react";
import { View, StyleSheet, Text } from "react-native";

const styles = StyleSheet.create({
  h1: {
    fontSize: 48,
  },
});

const SignupScreen = () => {
  return <Text style={styles.h1}>Signup Screen</Text>;
};

export default SignupScreen;
